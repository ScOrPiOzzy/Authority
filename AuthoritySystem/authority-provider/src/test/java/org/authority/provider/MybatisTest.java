package org.authority.provider;

public class MybatisTest {
//	@Test
//	public void testRecordMapper() throws Exception {
//		String resource = "mybatis/mapper/com/cas/authority/RecordMapper.xml";
//		InputStream inputStream = Resources.getResourceAsStream(resource);
//		SqlSessionFactory sqlSessionFactory = new SqlSessionFactoryBuilder().build(inputStream);
//		SqlSession session = sqlSessionFactory.openSession();
//		try {
////			UserMapper mapper = session.getMapper(UserMapper.class);
////			List<Article> articles = mapper.selectarticle(1);
////			for (Article article : articles) {
////				System.out.println(article.getId() + ":" + article.getTitle() + ":" + article.getContent() + " " + article.getUser());
////			}
//			RecordMapper mapper = session.getMapper(RecordMapper.class);
//			Record record = mapper.selectBy("we", "123");
//
//			System.out.println(record);
//
//		} finally {
//			// TODO: handle finally clause
//			session.close();
//		}
//	}
}
